import 'package:flutter/material.dart';
import 'package:hydroponics_app/feature/home_page.dart';

class HydroponicsApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: HomePage(),
    );
  }
}
